/**
 * Default URLs and placeholders used by command-palette operations.
 * These are centralized so consumers can override them via configuration.
 */

export const DEFAULT_COMMAND_SHARE_BASE_URL = "https://example.com/command";
export const DEFAULT_COMMANDS_SHARE_BASE_URL = "https://example.com/commands";
export const SYNC_ENDPOINT_PLACEHOLDER = "https://api.example.com";
