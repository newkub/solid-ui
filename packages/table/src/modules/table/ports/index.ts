// Table Ports Layer - Interface definitions for external dependencies
// The table module is self-contained with no external dependencies (no DB, HTTP, etc.)
// Ports will be added when external integrations are needed

export {};
